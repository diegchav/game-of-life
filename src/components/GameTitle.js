import React from 'react';

import StyledGameTitle from './styles/StyledGameTitle';

const GameTitle = () => (
  <StyledGameTitle>
    <p>Game of Life</p>
  </StyledGameTitle>
);

export default GameTitle;